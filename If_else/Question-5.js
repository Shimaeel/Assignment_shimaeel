let givenNumber = 7;
let rangeStart = 5;
let rangeEnd = 10;

// Check if givenNumber is in the range
if (givenNumber >= rangeStart && givenNumber <= rangeEnd) {
    console.log("number is present in the range");
} else {
    console.log("number is not present in the range");
}
